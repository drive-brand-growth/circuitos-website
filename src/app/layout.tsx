import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CircuitOS | AI-Powered Business Intelligence',
  description: 'Enterprise AI orchestration platform for intelligent business automation',
  keywords: 'AI, automation, business intelligence, enterprise, orchestration',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
